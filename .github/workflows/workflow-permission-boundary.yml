name: Workflow Permission Boundary

on:
  pull_request:
    paths:
      - '.github/workflows/**'
      - 'qa/check_workflow_permission_boundary.py'
  push:
    branches: [main]
    paths:
      - '.github/workflows/**'
      - 'qa/check_workflow_permission_boundary.py'

permissions:
  contents: read

jobs:
  workflow-permissions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate workflow permissions
        run: python3 qa/check_workflow_permission_boundary.py
