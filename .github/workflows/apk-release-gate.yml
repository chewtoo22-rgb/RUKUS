name: RUKUS APK Release Gate

on:
  pull_request:
  workflow_dispatch:

jobs:
  release-apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Runtime manifest contract tests
        run: python3 qa/test_verify_runtime_manifest.py

      - name: Release unit tests
        shell: bash
        run: |
          set +e
          gradle :app:testReleaseUnitTest --stacktrace 2>&1 | tee gradle-release-unit-test.log
          status=${PIPESTATUS[0]}
          if [[ $status -ne 0 ]]; then
            {
              echo '## Release unit-test failure tail'
              echo '```text'
              tail -n 140 gradle-release-unit-test.log
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
            exit "$status"
          fi

      - name: Upload release unit-test diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: gradle-release-unit-test-diagnostics
          if-no-files-found: error
          path: gradle-release-unit-test.log

      - name: Release lint
        shell: bash
        run: |
          set +e
          gradle :app:lintRelease --stacktrace 2>&1 | tee gradle-lint-release.log
          status=${PIPESTATUS[0]}
          if [[ $status -ne 0 ]]; then
            {
              echo '## Release lint failure tail'
              echo '```text'
              tail -n 180 gradle-lint-release.log
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
            exit "$status"
          fi

      - name: Upload release lint diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: gradle-release-lint-diagnostics
          if-no-files-found: warn
          path: |
            gradle-lint-release.log
            app/build/reports/lint-results-release.html
            app/build/reports/lint-results-release.xml
            app/build/reports/lint-results-release.txt

      - name: Assemble release variant
        run: gradle :app:assembleRelease --stacktrace

      - name: Capture packaged manifest evidence
        shell: bash
        run: |
          mkdir -p app/build/outputs/apk/release/evidence
          "$ANDROID_HOME/cmdline-tools/latest/bin/apkanalyzer" manifest print \
            app/build/outputs/apk/release/app-release-unsigned.apk \
            > app/build/outputs/apk/release/evidence/AndroidManifest.xml

      - name: Verify release APK structure and runtime wiring
        shell: bash
        run: |
          set +e
          bash qa/verify-apk-release.sh app/build/outputs/apk/release/app-release-unsigned.apk \
            2>&1 | tee app/build/outputs/apk/release/evidence/release-gate.log
          status=${PIPESTATUS[0]}
          if [[ $status -ne 0 ]]; then
            {
              echo '## Release APK gate failure'
              echo '```text'
              cat app/build/outputs/apk/release/evidence/release-gate.log
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
            exit "$status"
          fi

      - name: Upload release-gate diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: RUKUS-release-gate-diagnostics
          if-no-files-found: error
          path: |
            app/build/outputs/apk/release/app-release-unsigned.apk
            app/build/outputs/apk/release/evidence/AndroidManifest.xml
            app/build/outputs/apk/release/evidence/release-gate.log

      - name: Record SHA-256
        run: sha256sum app/build/outputs/apk/release/app-release-unsigned.apk | tee app/build/outputs/apk/release/SHA256SUMS.txt

      - uses: actions/upload-artifact@v4
        with:
          name: RUKUS-release-unsigned
          if-no-files-found: error
          path: |
            app/build/outputs/apk/release/app-release-unsigned.apk
            app/build/outputs/apk/release/SHA256SUMS.txt
            app/build/outputs/apk/release/evidence/AndroidManifest.xml
            app/build/outputs/apk/release/evidence/release-gate.log
